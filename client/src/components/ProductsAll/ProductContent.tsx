import styles from "./Products.module.scss";

const ProductContent = () => {
  return (
    <section className={styles["product-content"]}>ProductContent</section>
  );
};

export default ProductContent;
